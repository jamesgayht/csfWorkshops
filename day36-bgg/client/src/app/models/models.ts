export interface Game {
    id: number
    name: string
}

export interface GameDetails {
    id: number
    image: string
    name: string
    ranking: number
    url: string
    usersRated: number 
    year: number
}