export interface Inventory {
    description: string
    imageUrl: string
    quantity: number
}

export interface CustomerSelection {
    description: string
    imageUrl: string
    quantity: number
    index: number
}
